# 一键安装VLESS+Reality + Hysteria2 + TUIC

## 直接安装

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/makessr/one-times/main/makessr.sh) install
```

### 直接卸载

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/makessr/one-times/main/makessr.sh) uninstall
```
